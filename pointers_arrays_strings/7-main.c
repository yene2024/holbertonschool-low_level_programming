#include "main.h"

/**
 * puts_half - function that prints half of a string
 *
 * @str:half of a string
 */
int main(void)
{
    char *str;

    str = "0123456789";
    puts_half(str);
    return (0);
}
