#include "main.h"

/**
 * _puts - function that prints a string
 *@str: string to print
 */

int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}
