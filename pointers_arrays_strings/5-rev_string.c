#include "main.h"
#include <stdio.h>

/**
 * rev_string - function that reverse a string
 */
void rev_string(char *s)
{
	int length = 0;
	
	while (s[length] != '\0')
	{
	length++;
	}
	for (int i = length - 1; i >= 0; i--)
	{
	_putchar(s[i];
	}
	_putchar(s[i]);
}


