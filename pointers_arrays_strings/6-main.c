#include "main.h"

/**
 * puts2 - function that prints every other character
 *@str:starting with the first character
 *
 */
int main(void)
{
    char *str;

    str = "0123456789";
    puts2(str);
    return (0);
}
