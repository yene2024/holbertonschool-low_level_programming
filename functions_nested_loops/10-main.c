#include "main.h"
#include <stdio.h>
/**
 * add - function that adds two integers.
 * @a: number integers
 * @b: number integers
 * Return: the result
 */
int main(void)
{
    int n;

    n = add(89, 9);
    printf("%d\n", n);
    return (0);
}
