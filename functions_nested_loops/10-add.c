#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers.
 * @a: number integers
 * @b: number integers
 * Return: the result
 */
int add(int a, int b)
	{
		return (a + b);
	}
